/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['media.licdn.com',"upload.wikimedia.org","voogo.fr","ivonomad.com"],
  },
}

module.exports = nextConfig
